请连接并以此目录作为项目根目录：

`F:\Nextcloud\project\CadBridge`

当前阶段**不要实现业务代码**。你的任务是基于下面已经完成的架构头脑风暴，独立复核、补全并最终产出一套可以直接指导后续多个 AI 分模块开发的正式技术文档：

- `PRD.md`
- `DESIGN.md`
- `IMPLEMENTATION_PLAN.md`
- `ACCEPTANCE.md`

目标不是写泛泛而谈的文档，而是把 CadBridge 设计到“后续 AI 只需读这四份文档，就能按模块持续实施、测试和验收”的程度。

你拥有较高自主权：可以查阅最新 Autodesk 官方资料、MCP 官方文档和高质量 GitHub 项目，对现有决策做技术验证、补充遗漏、消除矛盾。优先复用官方 SDK、官方机制和成熟高水平开源设计，不重复造轮子。如果发现现有决策存在明确技术问题，可以调整，但必须在文档中说明证据、原因和替代方案；不要因为个人偏好重新推翻已经合理确定的架构。

---

## 一、项目目标

CadBridge 是一个面向 AI Agent、开发工具和自动化程序的 AutoCAD 控制与开发桥梁。

最终希望 AI 能够：

- 感知当前 AutoCAD、当前 DWG、布局、选择集和图元；
- 精确查询实体类型、Handle、Layer、几何、BoundingBox、文字、块属性等结构化信息；
- 创建、修改、删除 CAD 对象；
- 执行多步骤 Batch，并支持可靠事务；
- 加载、Reload、运行和测试 AutoLISP；
- 真正调试 AutoLISP，包括：
  - Breakpoint
  - Continue
  - Step Into
  - Step Over
  - Step Out
  - Watch
  - Locals
  - Evaluate
  - Call Stack
  - Break on Error
- 让 Coding Agent 可以形成：
  `修改 LSP → 加载 → 运行 → 断点 → 查看调用栈/变量 → 查询图面状态 → 修复 → 重测`
  的自动开发闭环；
- 第一版只支持 AutoCAD，但架构预留未来接入 ZWCAD 等 CAD 平台的能力。

---

# 二、已经确定的核心架构

除非有明确技术证据证明不可行，否则以下视为架构基线。

## M1 — CAD Runtime

采用：

**C# / .NET AutoCAD Plugin**

原因：

- 优先使用 Autodesk 官方 .NET API；
- 普通 CAD 数据库、图元、事务、文档操作覆盖足够；
- 比 ObjectARX 更容易让 AI 长期开发维护；
- V1 不为了少量自定义实体提前引入 C++/ObjectARX。

第三方/自定义实体：
- .NET 能访问则支持；
- 无法访问则识别并返回 unsupported；
- 只有未来出现明确需求时再考虑 native extension。

V1：仅 AutoCAD。

未来预留 ZWCAD Adapter，但不要为了未来平台把 Autodesk API 整套重新包装一遍。

---

## M2 — Host ↔ Plugin 内部通信

采用：

**Named Pipe**

这是内部 IPC，不对外暴露。

初始协议可采用：

`4-byte little-endian length + UTF-8 JSON`

Host 和 Plugin 必须通过稳定的协议 Contract 解耦各自 Runtime。

Named Pipe 还承担：
- 多 AutoCAD 实例；
- Session；
- Capability Handshake；
- Plugin/Host Protocol Version；
- request_id；
- Batch 请求与结构化响应。

---

## M3 — CadBridge Host

建议主方案：

**独立 C#/.NET Host**

优先采用：
- 官方 MCP C# SDK；
- ASP.NET Core；
- 官方/成熟库。

外部接口分工：

### MCP
主要服务 AI Agent。

长期主入口倾向：
**MCP Streamable HTTP**

同时可提供轻量：
`CadBridge.McpStdio`
作为兼容 shim。

### REST
主要服务：
- LSP 开发和调试；
- Python/Node/C# 等第三方程序；
- 自动化测试；
- GUI；
- 精确程序调用。

### CLI
主要服务：
- 人工调试；
- CI；
- Coding Agent；
- 脚本；
- 故障排查。

CLI 不直接连接 Plugin，必须经过 Host。

### Events
建议：
**SSE**

用于 Host → Client 主动事件，例如：

- debugger stopped；
- breakpoint hit；
- revision changed；
- document changed；
- batch progress；
- AutoCAD connected/disconnected。

V1 不必为了事件系统直接上复杂 WebSocket。

### 核心原则

MCP / REST / CLI **不能各自实现业务逻辑**。

全部进入同一：

`ToolRegistry → CommandService → BatchExecutor → PluginConnection`

---

# 三、M4 — Tool Contract / MCP Token 架构

这是关键设计。

已批准采用：

**Fine-grained Strongly Typed Tool Registry  
+ Essential Tools  
+ Search / Describe / Call Discovery  
+ Batch**

内部允许未来存在 100～500 个细粒度命令，例如：

- `entity.create.line`
- `entity.create.circle`
- `entity.fillet`
- `entity.trim`
- `query.entities`
- `layer.create`
- `block.insert`
- `lisp.load`
- `lisp.eval`
- `debug.step_into`
- `debug.stack`
- ...

每个内部 Tool/Command：
- 独立强类型 Schema；
- 明确输入输出；
- 可测试；
- 可做 Risk；
- 可判断 Transaction；
- 可判断 Batch；
- 可做 Capability Filter。

不要因为 Token 问题把内部 API 改成巨型字符串 DSL。

### 默认常驻 Essential Tools

建议固定约 7 个：

1. `cad_status`
2. `cad_query`
3. `cad_view`
4. `search_tools`
5. `describe_tools`
6. `call_tool`
7. `cad_batch`

普通实体操作不长期暴露给模型。

### Lisp 开发 Profile

`lisp-dev` 可在上述基础上增加少量高频工具，例如：

- `lisp_runtime`
- `debug_control`
- `debug_inspect`

不要长期暴露几十个 debugger tools。

### Discovery 参考

优先复用成熟思想：

- FastMCP Tool Search / BM25；
- U-C4N/Autocad-MCP 的 CAD aliases、AutoCAD command names、synonyms、risk ranking、compact search result；
- mcp-scout 的：
  - `search_tools`
  - `describe_tools`
  - `call_tool`
  - validation failure 返回 expected signature；
- GitHub MCP Server 的 Toolset/Profile 思想。

需要考虑中文 CAD synonyms。

### Result Token 同样重要

大数据查询必须支持：

- filter
- fields projection
- limit
- cursor/pagination
- summary

禁止默认把几万图元的全部属性返回给 AI。

---

# 四、M5 — Command / Batch / Transaction

核心模型：

**Command = 最小 CAD 操作  
Batch = Command 容器  
Transaction = Batch 的执行语义**

统一要求：

- 所有 Plugin 操作通过 `BatchEnvelope`；
- 单命令 = 一个 commands 长度为 1 的 Batch；
- 一个 Batch 通常对应一次 Named Pipe 调用；
- 写操作默认 `atomic`；
- 支持 `best_effort`；
- atomic V1 只允许真正能进入数据库 Transaction 的操作；
- Save / Plot / Open / Close / 外部程序等非事务副作用操作，不能伪装成可 rollback；
- Atomic Batch 若包含不支持事务的 Command，应在执行前拒绝；
- V1 不做跨文档分布式事务。

必须支持：

### Revision Guard

请求携带：

- `document_id`
- `expected_revision`

用户在 AI 查询以后修改图纸时，旧 Revision 的写操作应被拒绝：

`REVISION_CONFLICT`

### Idempotency

预留/实现：

`idempotency_key`

避免网络响应丢失后 Agent retry 产生重复实体。

### Step Reference

Batch 中后续步骤应该能够引用前一步结果，例如：

`$step1.handle`

### Structured Result

包含：

- batch_id
- revision_before
- revision_after
- committed
- 每一步 result
- failed_step
- 标准 error code

---

# 五、M6 — AutoLISP Runtime & Debugger

这是项目的核心能力之一。

不要只做：

`SendCommand("(load ...)")`

而应该形成正式 Lisp Runtime Service。

基础能力：

- load
- reload
- eval
- call
- run command
- output capture
- error capture
- timeout
- automated tests

正式 Debugger：

- Attach
- Breakpoint
- Continue
- Step Into
- Step Over
- Step Out
- Watch
- Locals
- Call Stack
- Evaluate
- Break on Error

优先复用 Autodesk 官方 AutoLISP Debug Adapter / DAP 能力。

不要自己实现 AutoLISP 单步解释器。

需要重点验证：

**CadBridge Host 是否可以脱离 VS Code UI，直接作为 DAP Client 驱动 Autodesk Debug Adapter。**

如果需要 PoC，请在 IMPLEMENTATION_PLAN 中把它设成早期技术验证 Gate。

调试通道和 CAD Query 通道应分离：

Debugger：
- Stack
- Locals
- Watch
- Step

Plugin：
- DWG
- 图元
- Selection
- Geometry
- Revision

目标是断点暂停时 AI 可以同时看到：

“程序执行到哪里”
+
“当前 CAD 数据库是什么状态”。

---

# 六、M7 — Headless

V1：

**不实现 Headless Backend。**

但架构预留：

`IRuntimeBackend`

V1：
`LiveAutoCadBackend`

未来优先考虑：
`CoreConsoleBackend`

更远期根据实际需求再考虑 APS / ezdxf 等。

不要因为未来 Backend 把 V1 做复杂。

---

# 七、M8 — AutoCAD 多版本

V1 目标：

### Legacy

AutoCAD **2015–2024**

- `net48`
- Legacy Plugin Shell

### Modern

AutoCAD **2025–2026**

- `net8.0-windows`
- Modern Plugin Shell

参考项目：

`https://github.com/moshouhot/batchPrintZWCAD`

重点研究：

- `src/AcadBatchPlot/AcadBatchPlot.csproj`
- `src/AcadBatchPlot.Core/AcadBatchPlot.Core.csproj`

该项目已经采用：

- AutoCAD 2015–2024：net48；
- AutoCAD 2025+：net8；
- 两套 csproj；
- Common + AutoCAD 共享源码；
- 项目文件/API 引用差异；
- 尽量复用业务主体。

CadBridge 采用类似思想：

**Two Shells + Shared Core/Shared Source + Thin Compat Layer**

但是需要比参考项目更严格：

- `#if NET48 / ACAD_CORE` 等条件编译尽量集中在 Compatibility/Shell 层；
- 不允许条件编译散落整个业务代码；
- 不复制两套 Command/Query/Batch/Lisp 业务逻辑。

真实支持结论必须来自真机兼容矩阵，不得仅凭编译成功宣称全部版本支持。

---

# 八、M9 — Portable / Bootstrap / Operations

用户明确要求：

**CadBridge 默认做绿色版。**

所有 CadBridge 文件尽量集中在：

`F:\...\CadBridge` 类似单一目录结构。

默认：

- 不 MSI；
- 不永久安装 Plugin；
- 不默认 `.bundle` 自动加载；
- 不修改 AutoCAD 永久启动项；
- 不要求用户每次手动 NETLOAD。

### 工作模式

Launcher：

1. 扫描已安装 AutoCAD；
2. 扫描当前运行中的 AutoCAD；
3. 用户选择某个 AutoCAD 或 PID；
4. 根据 CAD Version 自动选择：
   - 2015–2024 → Legacy DLL
   - 2025–2026 → Modern DLL
5. 临时执行 NETLOAD；
6. Plugin 建立 Named Pipe；
7. Host 连接；
8. MCP / REST / CLI Ready。

如果 AutoCAD 未启动：

Launcher 可以启动指定 AutoCAD，再等待 Ready 后加载 Plugin。

### COM 定位

COM **不作为 CAD Runtime**。

可以作为 Bootstrap Loader：

- attach running AutoCAD；
- 执行一次 NETLOAD；
- 完成后正式操作全部转 Named Pipe + .NET Plugin。

参考 TmAgent 的“连接已有 CAD → 加载 DLL”用户体验。

### 临时加载定义

Plugin 只在当前 AutoCAD 进程有效。

关闭 AutoCAD 后即结束。

不要把 Managed DLL 热卸载作为目标。

永久 AutoLoad 可以未来作为可选命令：

`cadbridge install-autoload`

但不是默认行为。

### Portable 数据

尽量：

```text
CadBridge/
├── config/
├── data/
├── logs/
├── runtime/
└── Plugin/
```

删除整个目录即可基本移除 CadBridge。

---

# 九、Connection / Version / Capability

Host 必须支持多个 AutoCAD 实例。

Session 至少包含：

- platform
- AutoCAD version
- PID
- active document
- Plugin version
- Protocol version
- Pipe endpoint
- capabilities

必须区分：

- HostVersion
- PluginVersion
- ProtocolVersion

建立协议版本协商，而不是简单 equality。

必须存在 Capability Handshake，例如：

- query
- transaction
- view_capture
- lisp.load
- lisp.eval
- breakpoint
- step_into
- step_over
- stack
- watch

Tool Registry 根据实际 Capability 过滤不可用能力。

---

# 十、日志与诊断

全链路统一：

`request_id`

贯穿：

MCP/REST/CLI  
→ Host  
→ CommandService  
→ Batch  
→ Named Pipe  
→ Plugin  
→ AutoCAD Transaction

结构化日志至少记录：

- request_id
- session_id
- batch_id
- document_id
- PID
- revision
- operation
- elapsed_ms
- success/error
- error_code

必须设计：

`cadbridge status`

以及：

`cadbridge doctor`

doctor 至少能够自动诊断：

- Host
- MCP
- REST
- AutoCAD 安装
- 当前 AutoCAD PID
- Plugin 是否加载
- Named Pipe
- Protocol compatibility
- active document
- Lisp Runtime
- Debug Adapter
- Legacy / Modern DLL
- 关键配置

支持机器可读输出，例如：

`cadbridge doctor --json`

方便 Coding Agent 自诊断。

---

# 十一、安全边界

V1 默认：

- localhost only；
- 不监听 0.0.0.0；
- Named Pipe current-user only；
- MCP/REST 要有适当的本地身份/Token方案；
- 对任意文件路径、Lisp 执行、Command 等高权限能力明确风险级别；
- Discovery 中区分：
  - read
  - write
  - destructive

避免默认开放远程控制 AutoCAD。

---

# 十二、测试体系

至少设计：

### L1 Unit Tests

- Tool Registry
- Discovery
- aliases
- schema
- batch validation
- revision
- result projection
- protocol

### L2 Contract Tests

确保：

MCP / REST / CLI

调用同一业务能力时具有一致 Contract。

### L3 Host Integration

使用 Fake Plugin 验证：

- Named Pipe
- disconnect
- timeout
- malformed response
- protocol mismatch
- multi-session
- Batch

### L4 Real AutoCAD Integration

至少建立代表版本测试矩阵，例如：

- 2015
- 2024
- 2025
- 2026

实际验证：

- 临时加载 Plugin
- Host connect
- query
- create/modify
- transaction
- batch
- LSP load/eval
- debugger

### L5 AI E2E

最终必须有真正 Agent 任务验证，例如：

CAD 操作：
“查找 WALL 图层满足条件的图元并修改，然后验证最终 DWG。”

LSP 调试：
“加载故障 LSP，通过 breakpoint/stack/watch/step 找出错误并修复。”

---

# 十三、官方和优秀项目优先原则

这是本项目的明确技术原则：

**P1 官方 SDK/API  
→ P2 高质量、活跃、许可证兼容的开源方案  
→ P3 成熟第三方库  
→ P4 自研**

重点研究并验证最新版本：

- Autodesk AutoCAD .NET API
- Autodesk AutoLISP Debug Adapter / DAP
- 官方 MCP C# SDK
- ASP.NET Core
- U-C4N/Autocad-MCP
- mcp-scout
- GitHub MCP Server
- bimwright/dwg-mcp
- beiming183-cloud/AutoCAD-MCP
- puran-water/autocad-mcp
- multiCAD-mcp
- batchPrintZWCAD
- TmAgent-cad 的公开设计资料

不要因为某项目 README 声称支持就直接作为事实；关键架构与兼容性尽量检查源码、官方文档或实际测试证据。

使用开源代码/数据时必须核查 License，并在设计中说明哪些内容可直接复用、哪些只借鉴思想。

---

# 十四、四份文档要求

## `PRD.md`

重点写：

- 项目解决什么问题；
- 核心用户；
- 典型使用场景；
- V1 范围；
- 明确 Non-Goals；
- 功能需求；
- LSP Debug 用户流程；
- AI CAD 感知流程；
- Portable 使用流程；
- AutoCAD 2015–2026 支持边界；
- V1 / Future 分界；
- 产品级成功标准。

不要把 PRD 写成实现文档。

---

## `DESIGN.md`

这是最重要的一份。

必须包含：

- 总体架构图；
- M1–M9 模块关系；
- Solution / Project 建议拆分；
- Host 内部架构；
- Plugin 内部架构；
- Legacy/Modern Shell；
- Named Pipe Contract；
- Session/Connection Manager；
- Command Registry；
- Tool Registry；
- Discovery；
- Batch/Transaction；
- Revision Guard；
- Idempotency；
- Query；
- View；
- Lisp Runtime；
- DAP Debug Coordinator；
- MCP；
- REST；
- CLI；
- SSE；
- Portable Bootstrap；
- Capability Handshake；
- Protocol Versioning；
- Error Model；
- Logging；
- Security Boundary；
- Extension points；
- Future ZWCAD；
- Future CoreConsole。

关键 DTO / Envelope / API 可以给出 JSON 示例和接口草案。

设计应足够明确，但不要为了“详细”提前把每个类和函数全部写死。

---

## `IMPLEMENTATION_PLAN.md`

要求能够让后续多个 AI **分阶段、分模块独立实施**。

建议按照依赖关系拆 Milestone/Gate，而不是一次实现全部。

必须明确：

- 每阶段目标；
- 输入依赖；
- 产出；
- 验证方式；
- 哪些阶段可并行；
- 哪些是强制技术 Gate。

特别需要尽早验证高风险问题，例如：

- Legacy / Modern 最小 Plugin；
- Named Pipe；
- AutoCAD 临时 NETLOAD；
- Official C# MCP SDK；
- MCP Discovery；
- AutoLISP Runtime；
- CadBridge Host 作为 DAP Client 是否能驱动 Autodesk Debug Adapter；
- 真机 Revision；
- 多 AutoCAD Session。

优先先做最小垂直链路，再逐渐扩能力。

不要第一阶段就实现 100 个 CAD 命令。

---

## `ACCEPTANCE.md`

禁止写：

“功能正常”
“测试通过”

这种不可验证条款。

必须给出证据化 Gate。

例如：

- 什么命令；
- 什么输入；
- 预期 JSON；
- AutoCAD 中什么实际变化；
- Revision 应如何变化；
- Rollback 后 DWG 应保持什么状态；
- 断点应该停在哪；
- Stack / Watch 应返回什么；
- Plugin 加载应如何确认；
- 哪些 AutoCAD 真机版本必须测试；
- Token/Tool Discovery 如何测；
- 失败场景怎么测。

建议把：

`Unit → Contract → Fake Plugin → Real AutoCAD → AI E2E`

全部形成明确 Gate。

最终只有证据齐全才能宣布 V1 完成。

---

# 十五、工作方式

请先检查项目目录和已有文件。

如果目录为空，就直接从上述架构基线开始。

你可以自主：

- 搜索官方文档；
- 检查 GitHub 源码；
- 比较方案；
- 修正文档结构；
- 补充遗漏；
- 发现架构冲突；
- 给关键决策写 ADR 风格说明。

不要因为小问题频繁询问我。

只有以下情况才停下来询问：

- 缺少决定项目路线的关键信息；
- 需要破坏性操作；
- 发现现有架构存在根本性问题，继续写文档会造成明显返工。

否则自主完成。

---

# 十六、完成标准

最终不是简单告诉我“4 个 MD 已生成”。

完成前请做一次独立一致性审计，确认：

1. 四份文档互不矛盾；
2. PRD 的每个 V1 核心需求，在 DESIGN 中有技术承载；
3. DESIGN 中的每个 V1 关键模块，在 IMPLEMENTATION_PLAN 中有实施阶段；
4. 每个 V1 核心能力，在 ACCEPTANCE 中有可执行验收证据；
5. V1 和 Future Scope 没有混淆；
6. AutoCAD 2015–2024/net48 和 2025–2026/net8 边界一致；
7. Portable 临时加载策略贯穿设计和验收；
8. MCP/REST/CLI 没有重复业务逻辑；
9. M4 Token Strategy 被实际落实，而不是只写理念；
10. Lisp Debugger 包含真正的 Breakpoint / Step / Watch / Call Stack；
11. 所有高风险技术假设都有明确 PoC/Gate；
12. 官方 SDK和引用项目的使用方式、来源、许可证风险有说明。

完成后给我一个简短总结：

- 创建/修改了哪些文件；
- 最关键的架构结论；
- 你对原方案做了哪些调整；
- 哪些仍属于技术风险或需要 PoC 验证；
- 是否已经达到“可以交给下一批 AI 分模块实施”的状态。

**本阶段不要开始正式业务实现。**